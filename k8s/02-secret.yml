apiVersion: v1
kind: Secret

metadata:
  name: talenthub-secret

type: Opaque

stringData:
  DB_USERNAME: "talenthub_user"
  DB_PASSWORD: "demo-password-only"
  JWT_SECRET: "demo-jwt-secret"
  API_TOKEN: "demo-api-token"
